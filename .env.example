# ArabicMind Configuration
HOST=127.0.0.1
PORT=7071
DEBUG=false
SECRET_KEY=change-me-in-production

# Default provider config (can also be set per-request in the UI)
# These are optional — keys can be entered directly in the browser UI
DEFAULT_TR_PROVIDER=anthropic
DEFAULT_TR_MODEL=claude-haiku-4-5-20251001
DEFAULT_RS_PROVIDER=anthropic
DEFAULT_RS_MODEL=claude-opus-4-6
